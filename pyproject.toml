[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "health"
version = "0.1.0"
description = "Facetwork domain: disease-burden choropleths (cancer/diabetes/Alzheimer's/stroke)"
requires-python = ">=3.11"
authors = [{name = "Ralph Lemke"}]
# openpyxl: AIDSVu publishes the state HIV series as per-year .xlsx workbooks
dependencies = ["requests", "shapely", "openpyxl"]

[project.entry-points."facetwork.domains"]
health = "health:domain"

[tool.setuptools.packages.find]
where = ["src"]
